import { StyleSheet } from "react-native";

const SplashStyles = StyleSheet.create({
    splashcontainer: {
      flex: 1,
    },
    splash: {
      width: '100%',
      height: '100%'
    },
  });

  export default SplashStyles;